import {createFlow} from "@builderbot/bot";
import {initFlowWelcome} from "~/bot/template/init-flow";


export const initFlow = createFlow([initFlowWelcome]);
//export const initFlow = createFlow([]);